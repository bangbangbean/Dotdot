<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.dotdot">
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality.
    -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-feature android:name="android.hardware.camera.autofocus" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.FullScreen"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".QrcodeScannerNext"></activity>


        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" /> <!-- ACTIVITIES -->
        <activity
            android:name=".StoreCreatCoupon"
            android:theme="@style/AppTheme.CreatCoupon" />
        <activity
            android:name=".PopActivity"
            android:theme="@style/AppTheme.Pop" />
        <activity
            android:name=".QrcodeCreate"
            android:theme="@style/AppTheme.PopMe" />
        <activity
            android:name=".member_storemode_record"
            android:theme="@style/AppTheme.PopMe" />
        <activity android:name=".QrcodeScanner" />
        <activity android:name=".Collectioncard" />
        <activity android:name=".botnav" />
        <activity android:name=".test" />
        <activity android:name=".Login" />
        <activity android:name=".Verification" />
        <activity android:name=".Forgetpassword" />
        <activity android:name=".Storeregist" />
        <activity android:name=".MainActivity" />
        <activity android:name=".Forgetpassword_2" />
        <activity android:name=".Forgetpassword_1" />
        <activity android:name=".Regist" />
        <activity android:name=".StoreCreatLoyaltyCard" />
        <activity
            android:name=".MemberIndex"
            android:label="@string/title_activity_maps" />
        <activity
            android:name=".StoreIndex"
            android:label="@string/title_activity_store_index"/>
        <activity android:name=".TransactionRecord"/>
        <activity android:name=".StoreOverlookCoupon">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>